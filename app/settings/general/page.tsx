/**
 * File overview:
 * Contains UI or data logic for a specific feature in Biodiversity Hub.
 * Main exports here are consumed by Next.js routes or shared components.
 */

import { TypographyP } from "@/components/ui/typography";

export default function GeneralSettings() {
  // Placeholder content for general settings until full controls are implemented.
  // Keeping the route in place avoids broken links while the page evolves.
  return <TypographyP>Edit general settings</TypographyP>;
}
